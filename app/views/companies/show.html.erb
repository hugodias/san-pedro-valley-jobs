<% content_for(:title, "#{@company.title}") %>

<div class="section-colored-top">
  <div class="container">
    <div class="section-job__header">
      <h1 class="section-job__header__title">
        <%= @company.title %>
      </h1>
      <h3 class="section-job__header__subtitle">
        Vagas disponíveis
      </h3>
    </div>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <div class="card">
        <div class="card-content">
          <% @company.jobs.each do |job| %>
            <%= render 'jobs/job_row', job: job %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
